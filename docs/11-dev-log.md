# 개발 기록

## 2026-05-21 프로젝트 기본 구조 생성

### 작업 목적

문서 초안 정리가 끝난 뒤 실제 구현을 시작하기 위한 기본 폴더 구조를 만들었다.

이번 단계에서는 기능 구현보다 프론트엔드와 백엔드가 들어갈 위치를 먼저 나누는 것을 목표로 했다.

### 작업 내용

- `frontend` 폴더를 생성했다.
- `React + TypeScript + Vite` 기반 프론트엔드 프로젝트를 생성했다.
- 프론트엔드 의존성을 설치했다.
- `backend` 폴더를 생성했다.
- FastAPI 백엔드 기본 구조를 만들었다.
- 백엔드에 `/health` 확인용 API를 추가했다.
- 루트 `.gitignore`를 추가했다.

### 현재 구조

```text
youtube-mood-recommendation
├─ backend
├─ docs
└─ frontend
```

### 확인한 것

- 프론트엔드 빌드가 성공하는 것을 확인했다.
- 백엔드는 Python 실행 환경이 없어 아직 실행하지 못했다.

### 남은 작업

- Python 설치 또는 실행 경로 설정
- FastAPI 서버 실행 확인
- 프론트엔드 기본 화면 정리
- 임시 AI 분석 API 추가
- 프론트엔드와 백엔드 연결

## 2026-05-21 프론트엔드 기본 화면 정리

### 작업 목적

Vite 기본 화면을 제거하고 문서에서 정한 한 페이지 구조에 맞는 초기 화면을 만들었다.

### 작업 내용

- 소개 영역을 추가했다.
- 감정이나 상황을 입력하는 입력 영역을 추가했다.
- 추천 결과가 표시될 결과 영역을 추가했다.
- 입력값을 제출하면 임시 태그 예시가 보이도록 했다.
- 모바일 화면에서 입력 버튼과 결과 영역이 세로로 보이도록 스타일을 정리했다.

### 확인한 것

- `npm run lint`가 통과했다.
- `npm run build`가 통과했다.

### 남은 작업

- 백엔드 임시 분석 API 추가
- 프론트엔드에서 실제 API 호출 연결
- 추천 결과 카드 구조 설계

## 2026-05-21 추천 결과 화면 분리

### 작업 목적

추천 음악과 플레이리스트를 더 넓게 보여주기 위해 입력 화면과 추천 결과 화면을 분리했다.

### 작업 내용

- 입력 후 추천 결과 화면으로 전환되도록 화면 상태를 나눴다.
- 추천 결과 화면에 입력 문장과 분위기 태그 예시를 표시했다.
- 다시 입력하기 버튼으로 메인 화면에 돌아갈 수 있게 했다.
- 사용자 흐름, 화면 초안, 메뉴 트리 문서를 결과 화면 분리 구조에 맞게 수정했다.

### 확인할 것

- 입력 후 결과 화면으로 전환되는지 확인한다.
- 다시 입력하기 버튼으로 입력 화면에 돌아오는지 확인한다.

## 2026-05-21 백엔드 임시 분석 API 추가

### 작업 목적

프론트엔드가 사용자 입력 문장을 백엔드로 보낼 수 있도록 임시 분석 API를 만들었다.

이 단계에서는 실제 AI 분석을 하지 않고, 나중에 AI 모델로 교체할 수 있는 응답 구조를 먼저 정했다.

### 작업 내용

- `POST /api/analyze` API를 추가했다.
- 요청 데이터로 사용자 입력 문장을 받도록 했다.
- 응답 데이터에 원본 문장, 감정, 분위기 태그, 검색 키워드를 포함했다.
- 실제 AI 연결 전까지 사용할 임시 분석 함수를 만들었다.
- 백엔드 README에 현재 API 설명과 요청/응답 예시를 추가했다.

### 확인한 것

- `POST /api/analyze` 요청이 정상 응답하는 것을 확인했다.

### 남은 작업

- 프론트엔드에서 백엔드 분석 API 호출 연결
- 결과 화면에서 백엔드 응답 데이터 표시

## 2026-05-21 프론트엔드와 백엔드 분석 API 연결

### 작업 목적

사용자가 입력한 문장을 프론트엔드에서 백엔드로 보내고, 백엔드가 반환한 분석 결과를 화면에 표시하도록 연결했다.

### 작업 내용

- 백엔드에 프론트엔드 개발 서버를 허용하는 CORS 설정을 추가했다.
- 프론트엔드 입력 제출 시 `POST /api/analyze` 요청을 보내도록 했다.
- 임시 태그 대신 백엔드 응답의 감정, 분위기 태그, 검색 키워드를 표시하도록 바꿨다.
- 요청 중 상태와 실패 메시지를 최소한으로 추가했다.

### 확인할 것

- 백엔드 서버가 켜진 상태에서 문장을 입력하면 결과 화면으로 이동하는지 확인한다.
- 결과 화면에 감정, 분위기 태그, 검색 키워드가 표시되는지 확인한다.

### 남은 작업

- YouTube API 연결 방식 정리
- 추천 결과 카드 구조 설계

## 2026-05-21 추천 결과 카드 구조 추가

### 작업 목적

YouTube API를 연결하기 전에 추천 결과를 어떤 형태로 보여줄지 화면 구조를 먼저 만들었다.

### 작업 내용

- 추천 결과 화면을 왼쪽 분석 요약 영역과 오른쪽 추천 결과 영역으로 나눴다.
- 왼쪽 영역에 입력 문장, 감정, 분위기 태그, 추천 유형 탭을 배치했다.
- `한 곡 추천` 탭과 `플레이리스트` 탭을 추가했다.
- 임시 추천 데이터로 곡 카드와 플레이리스트 카드를 표시했다.
- 모바일 화면에서는 한 줄 구조가 세로 구조로 바뀌도록 정리했다.

### 확인할 것

- 결과 화면에서 탭을 바꾸면 표시되는 추천 카드가 바뀌는지 확인한다.
- 모바일 폭에서도 분석 요약과 추천 카드가 자연스럽게 세로로 배치되는지 확인한다.

### 남은 작업

- YouTube API 응답 데이터 구조 정리
- 임시 추천 데이터를 실제 YouTube 검색 결과로 교체

## 2026-05-21 YouTube 추천 응답 구조 준비

### 작업 목적

실제 YouTube API를 연결하기 전에 백엔드가 프론트엔드에 넘길 추천 결과 데이터 구조를 먼저 정리했다.

### 작업 내용

- 백엔드 요청과 응답 모델을 `schemas.py`로 분리했다.
- YouTube 추천 데이터 역할을 `youtube.py`로 분리했다.
- 분석 API 응답에 `recommended_track`과 `recommended_playlists`를 추가했다.
- 프론트엔드가 임시 상수 대신 백엔드 응답의 추천 데이터를 표시하도록 변경했다.
- 백엔드 README에 파일 역할과 확장된 응답 예시를 추가했다.

### 확인할 것

- 분석 API 응답에 추천 곡과 플레이리스트 데이터가 포함되는지 확인한다.
- 프론트엔드 결과 카드가 백엔드 응답 데이터를 기준으로 표시되는지 확인한다.

### 남은 작업

- YouTube API 키 설정 방식 추가
- `youtube.py`의 임시 데이터를 실제 YouTube 검색 결과로 교체

## 2026-05-21 YouTube API 키 설정 방식 추가

### 작업 목적

YouTube API 키를 코드에 직접 작성하지 않도록 환경 변수 설정 방식을 준비했다.

### 작업 내용

- `backend/.env.example` 파일을 추가했다.
- `YOUTUBE_API_KEY` 환경 변수 이름을 정했다.
- 백엔드 README에 `.env` 설정 방법을 추가했다.

### 확인한 것

- `.gitignore`에서 실제 `.env` 파일은 제외되고 `.env.example`만 Git에 포함될 수 있도록 되어 있다.

### 남은 작업

- YouTube API 키 발급
- 백엔드에서 `.env` 값을 읽는 방식 추가
- 실제 YouTube 검색 API 호출 구현

## 2026-05-22 백엔드 환경 변수 로딩 추가

### 작업 목적

백엔드에서 `.env` 파일에 저장한 YouTube API 키를 읽을 수 있도록 준비했다.

### 작업 내용

- `python-dotenv`를 백엔드 의존성에 명시했다.
- `app/config.py`를 추가해 `backend/.env` 파일을 읽도록 했다.
- `YOUTUBE_API_KEY` 값을 코드에서 사용할 수 있게 했다.
- `youtube.py`에서 API 키가 있는지 확인할 수 있는 함수를 추가했다.
- 백엔드 README에 `config.py` 역할과 환경 변수 사용 방식을 보충했다.

### 확인할 것

- `.env` 파일이 없어도 백엔드 앱이 정상 import되는지 확인한다.
- 실제 YouTube API 호출 단계에서 `YOUTUBE_API_KEY`를 사용하도록 연결한다.

### 남은 작업

- YouTube API 키 발급
- 실제 YouTube 검색 API 호출 구현

## 2026-05-22 YouTube 검색 API 호출 추가

### 작업 목적

백엔드에서 YouTube Data API를 호출해 추천 곡과 플레이리스트를 가져올 수 있도록 했다.

### 작업 내용

- `requests`를 백엔드 의존성에 추가했다.
- `youtube.py`에 YouTube 검색 요청 함수를 추가했다.
- 한 곡 추천은 `video` 검색 결과를 사용하도록 했다.
- 플레이리스트 추천은 `playlist` 검색 결과를 사용하도록 했다.
- YouTube 응답을 `RecommendationItem` 구조로 변환하도록 했다.
- API 키가 없거나 요청이 실패하면 기존 임시 추천 데이터를 반환하도록 했다.
- 백엔드 README에 실제 호출과 fallback 동작을 반영했다.

### 확인할 것

- `.env`에 YouTube API 키가 있을 때 실제 검색 결과가 반환되는지 확인한다.
- API 키가 없거나 요청이 실패해도 임시 추천 데이터가 반환되는지 확인한다.

### 남은 작업

- 프론트엔드 추천 카드에 YouTube 링크 연결
- 썸네일 표시 여부 결정

## 2026-05-22 추천 카드 YouTube 링크 연결

### 작업 목적

백엔드가 내려주는 YouTube 추천 결과를 사용자가 실제로 열어볼 수 있도록 했다.

### 작업 내용

- 한 곡 추천 카드에 `YouTube에서 열기` 링크를 추가했다.
- 플레이리스트 카드에 `YouTube에서 열기` 링크를 추가했다.
- `thumbnail_url`이 있으면 카드 상단에 썸네일을 표시하도록 했다.
- 썸네일이 없으면 기존 텍스트 카드 구조가 유지되도록 했다.

### 확인할 것

- 추천 카드의 링크를 누르면 새 탭에서 YouTube가 열리는지 확인한다.
- 실제 YouTube API 결과에 썸네일이 포함되면 카드에 이미지가 표시되는지 확인한다.

### 남은 작업

- 실제 화면에서 YouTube API 결과 확인
- 추천 결과 UI 세부 조정

## 2026-05-22 음악 앱 스타일 UI 리디자인

### 작업 목적

추천 결과 화면을 참고 이미지처럼 음악 앱 대시보드에 가까운 형태로 변경했다.

### 작업 내용

- 전체 화면을 다크 톤의 앱 레이아웃으로 변경했다.
- 결과 화면에 왼쪽 사이드바와 오른쪽 메인 콘텐츠 영역을 만들었다.
- `Mood` 영역에 입력 문장, 감정, 분위기 태그를 배치했다.
- 추천 곡은 큰 히어로 카드로 표시하도록 변경했다.
- 플레이리스트는 음악 앱의 목록형 카드처럼 보이도록 변경했다.
- 하단에 현재 추천 곡을 보여주는 플레이어 바 형태의 영역을 추가했다.
- 모바일 화면에서는 대시보드가 세로 구조로 전환되도록 정리했다.

### 확인할 것

- 결과 화면이 참고 이미지와 비슷한 음악 앱 구조로 보이는지 확인한다.
- 입력 화면과 결과 화면이 같은 다크 톤으로 자연스럽게 이어지는지 확인한다.
- 모바일 화면에서 사이드바와 카드가 깨지지 않는지 확인한다.

### 남은 작업

- 실제 브라우저에서 UI 확인 후 세부 간격 조정

## 2026-05-22 추천 곡 목록 구조 변경

### 작업 목적

`한 곡 추천`을 하나의 곡만 보여주는 구조가 아니라, 참고 이미지처럼 여러 추천 곡을 보여주는 구조로 변경했다.

### 작업 내용

- 백엔드 응답 필드를 `recommended_track`에서 `recommended_tracks`로 변경했다.
- YouTube 영상 검색 결과를 최대 5개까지 가져오도록 변경했다.
- 검색 실패 시 사용할 임시 추천 곡도 여러 개로 변경했다.
- 프론트엔드 탭 이름을 `한 곡 추천`에서 `추천 곡`으로 변경했다.
- 첫 번째 추천 곡은 대표 카드와 하단 플레이어 바에 표시하도록 했다.
- 나머지 추천 곡은 작은 카드 목록으로 함께 표시하도록 했다.
- README의 응답 예시를 여러 추천 곡 구조에 맞게 수정했다.

### 확인할 것

- 추천 곡 탭에서 대표 곡과 추가 추천 곡이 함께 표시되는지 확인한다.
- 하단 플레이어 바가 첫 번째 추천 곡을 기준으로 표시되는지 확인한다.

### 남은 작업

- 실제 브라우저에서 추천 곡 목록 UI 확인

## 2026-05-22 추천 곡 화면 배치 조정

### 작업 목적

참고 이미지의 `Explore new`와 `Popular` 구조에 맞게 추천 곡 화면 배치를 다시 조정했다.

### 작업 내용

- `Explore new` 역할의 추천 곡 영역에 여러 곡을 앨범 카드 형태로 배치했다.
- 추천 곡 카드를 클릭하면 선택한 곡이 대표곡으로 바뀌도록 했다.
- `Popular` 역할의 상세 영역에 선택한 대표곡 이미지, 제목, 채널명, 추천 이유, YouTube 링크를 표시했다.
- 하단 플레이어 바도 선택한 대표곡을 기준으로 표시되도록 했다.
- 기존처럼 큰 대표 카드 하나가 먼저 나오고 아래로 길어지는 구조를 제거했다.

### 확인할 것

- 추천 곡 카드 클릭 시 대표곡 상세 영역과 하단 플레이어 바가 함께 바뀌는지 확인한다.
- 추천 곡 영역이 세로로 길게 늘어지지 않고 앨범 카드처럼 보이는지 확인한다.

### 남은 작업

- 실제 브라우저에서 참고 이미지와 비교하며 간격과 비율 조정

## 2026-05-22 추천 결과 레이아웃 재배치

### 작업 목적

참고 이미지처럼 추천 곡 영역이 오른쪽 메인 영역을 넓게 차지하고, Mood 영역은 아래 상세 영역에만 배치되도록 구조를 조정했다.

### 작업 내용

- 결과 화면 상단의 설명성 헤더 문구를 제거했다.
- 추천 곡 앨범 카드 영역을 메인 콘텐츠의 전체 폭에 배치했다.
- 선택한 대표곡 상세 영역과 Mood 영역을 그 아래 2컬럼으로 배치했다.
- Mood 영역이 추천 곡 상단 영역을 침범하지 않도록 구조를 분리했다.

### 확인할 것

- 추천 곡 카드 영역이 오른쪽 범위까지 넓게 차지하는지 확인한다.
- Mood 영역이 아래쪽 상세 영역에만 표시되는지 확인한다.

## 2026-05-22 플레이리스트 선택 구조 개선

### 작업 목적

플레이리스트 탭에서도 선택한 플레이리스트가 `Popular` 영역에 크게 표시되도록 추천 곡 탭과 같은 흐름으로 맞췄다.

### 작업 내용

- 선택한 플레이리스트 상태를 추가했다.
- 플레이리스트 목록을 클릭 가능한 항목으로 변경했다.
- 플레이리스트를 클릭하면 `Popular` 영역에 해당 플레이리스트 상세가 표시되도록 했다.
- 하단 플레이어 바도 현재 선택한 곡 또는 플레이리스트 기준으로 표시되도록 변경했다.
- 선택된 플레이리스트 항목에 강조 스타일을 추가했다.

### 확인할 것

- 플레이리스트 탭에서 항목을 클릭하면 `Popular` 영역이 바뀌는지 확인한다.
- 하단 플레이어 바가 선택한 플레이리스트 기준으로 바뀌는지 확인한다.

## 2026-05-22 플레이리스트 상세 표시 개선

### 작업 목적

플레이리스트 탭의 `Popular` 영역을 플레이리스트 상세에 맞는 구조로 바꿨다.

### 작업 내용

- 플레이리스트 탭에서는 추천 이유 대신 임시 음악 리스트 예시를 표시하도록 했다.
- 선택한 플레이리스트의 이미지, 제목, 채널명, 음악 리스트, YouTube 링크를 `Popular` 영역에 표시했다.
- 음악 리스트 예시는 나중에 YouTube `playlistItems` API로 실제 곡 목록을 가져오기 전까지 사용하는 임시 데이터다.

### 확인할 것

- 플레이리스트 탭에서 `Popular` 영역에 음악 리스트 예시가 표시되는지 확인한다.
- `YouTube에서 열기` 버튼이 플레이리스트 상세 안에 유지되는지 확인한다.

## 2026-05-26 플레이리스트 카드형 레이아웃 변경

### 작업 목적

플레이리스트 탭의 상단 목록도 추천 곡 탭처럼 이미지 중심의 카드 레이아웃으로 맞췄다.

### 작업 내용

- 플레이리스트 상단 영역을 목록형에서 앨범 카드형 가로 레일로 변경했다.
- 플레이리스트 카드에 썸네일, 제목, 채널명을 표시했다.
- 선택된 플레이리스트 카드에 강조 스타일을 적용했다.
- 더 이상 사용하지 않는 플레이리스트 목록형 CSS를 제거했다.

### 확인할 것

- 플레이리스트 탭에서 카드들이 추천 곡 탭처럼 가로로 표시되는지 확인한다.
- 플레이리스트 카드를 클릭하면 `Popular` 영역이 선택한 플레이리스트 기준으로 바뀌는지 확인한다.

## 2026-05-26 추천 카드 개수와 드래그 스크롤 확장

### 작업 목적

추천 곡과 플레이리스트 카드가 적어 화면이 비어 보이지 않도록 결과 개수를 늘리고, 카드 레일을 마우스로 드래그해 넘길 수 있게 했다.

### 작업 내용

- YouTube 추천 곡 검색 결과를 최대 10개까지 가져오도록 변경했다.
- YouTube 플레이리스트 검색 결과를 최대 10개까지 가져오도록 변경했다.
- API 실패 시 사용하는 임시 추천 곡과 플레이리스트 데이터도 10개로 늘렸다.
- 추천 곡 카드 레일에 마우스 드래그 스크롤을 추가했다.
- 플레이리스트 카드 레일에도 같은 드래그 스크롤을 적용했다.

### 확인할 것

- 추천 곡과 플레이리스트가 더 많이 표시되는지 확인한다.
- 카드 영역을 마우스로 잡고 좌우로 드래그할 수 있는지 확인한다.

## 2026-05-26 결과 화면 상단 검색 바 추가

### 작업 목적

결과 화면에서 처음 화면으로 돌아가지 않고도 새로운 감정이나 상황 문장을 다시 입력해 추천 결과를 갱신할 수 있게 했다.

### 작업 내용

- 결과 화면 상단에 자연어 검색 바를 추가했다.
- 시작 화면과 결과 화면 검색 바가 같은 분석 요청 흐름을 사용하도록 정리했다.
- 새 문장을 검색하면 추천 곡, 플레이리스트, Mood 결과, 선택된 대표 항목이 새 결과 기준으로 갱신되도록 했다.
- 역할이 겹치는 `다시 입력하기` 버튼을 제거했다.
- 모바일 화면에서는 검색 바가 세로로 배치되도록 반응형 스타일을 추가했다.

### 확인할 것

- 결과 화면 상단 검색 바에 새 문장을 입력하고 검색하면 결과가 갱신되는지 확인한다.
- 검색 후 첫 번째 추천 곡과 첫 번째 플레이리스트가 기본 선택되는지 확인한다.
- 모바일 폭에서도 검색 바가 화면 밖으로 넘치지 않는지 확인한다.

## 2026-05-26 하단 YouTube 플레이 바 구조 추가

### 작업 목적

하단 영역을 단순 링크 표시가 아니라 선택한 추천 곡을 재생할 수 있는 플레이 바 구조로 바꿨다.

### 작업 내용

- 선택한 추천 곡의 YouTube 영상 ID를 추출하는 함수를 추가했다.
- 재생 버튼을 누르면 선택한 곡의 YouTube embed iframe이 생성되도록 했다.
- 이전/다음 버튼으로 추천 곡을 이동할 수 있게 했다.
- 하단 바를 참고 이미지처럼 컨트롤, 곡 정보, 진행 바, YouTube 링크 영역으로 나눴다.
- 실제 YouTube 영상 ID가 없는 임시 데이터에서는 재생 버튼이 비활성화되도록 했다.

### 확인할 것

- 추천 곡 탭에서 실제 YouTube API 결과를 선택했을 때 재생 버튼이 활성화되는지 확인한다.
- 재생 버튼을 누르면 브라우저 정책에 따라 iframe 재생이 시작되는지 확인한다.
- 이전/다음 버튼을 눌렀을 때 선택 곡이 바뀌는지 확인한다.

## 2026-05-26 YouTube 진행 바 연동

### 작업 목적

하단 플레이 바의 진행 시간이 실제 YouTube 재생 상태와 연결되도록 했다.

### 작업 내용

- YouTube IFrame Player API 스크립트를 로드하도록 했다.
- 선택한 추천 곡을 숨겨진 YouTube Player 객체로 재생하도록 변경했다.
- 재생 중 현재 시간과 전체 길이를 주기적으로 읽어 진행 바와 시간 텍스트에 반영했다.
- `Popular` 카드 내부 정보가 카드 밖으로 넘치지 않도록 텍스트와 버튼 영역의 overflow 처리를 보강했다.
- 플레이리스트 상세에 임시 문구 대신 현재 추천 곡 제목을 표시하도록 연결했다.

### 확인할 것

- 재생 버튼을 누르면 진행 시간이 증가하는지 확인한다.
- 진행 바가 실제 재생 시간에 맞춰 이동하는지 확인한다.
- `Popular` 카드의 제목, 작성자, 버튼이 카드 밖으로 넘치지 않는지 확인한다.

## 2026-05-26 플레이리스트 트랙 요약 표시

### 작업 목적

플레이리스트 상세에서 트랙 목록이 너무 길어지는 문제를 줄이고, 핵심 곡만 먼저 보이도록 정리했다.

### 작업 내용

- 플레이리스트 응답에 `playlist_tracks` 필드를 추가했다.
- 플레이리스트 상세는 최대 3곡까지만 노출하도록 변경했다.
- 3곡을 초과하는 경우에는 `외 N개`로 남은 개수를 표시한다.
- 프론트는 플레이리스트 전용 목록을 우선 사용하고, 값이 없을 때만 임시 목록을 쓰도록 정리했다.

### 확인할 것

- 플레이리스트 탭에서 곡이 3개까지만 보이는지 확인한다.
- 3개보다 많으면 `외 N개`가 함께 표시되는지 확인한다.
- `recommended_tracks`가 아니라 `playlist_tracks` 기준으로 렌더링되는지 확인한다.

## 2026-05-26 플레이리스트 세로 목록과 재생 동기화

### 작업 목적

플레이리스트 상세에서 곡 목록을 세로 스크롤로 보여주고, 하단 재생 컨트롤과 현재 재생 곡 상태를 연결했다.

### 작업 내용

- 플레이리스트 상세를 세로 스크롤 가능한 목록으로 바꿨다.
- 목록은 기본적으로 몇 개만 보이고, 아래로 드래그하거나 스크롤하면 나머지 곡을 확인할 수 있게 했다.
- 현재 재생 중인 플레이리스트 곡은 목록에서 강조 표시되도록 했다.
- 하단의 이전/다음 버튼으로 곡이 바뀌면 목록도 같은 인덱스를 따라가도록 연결했다.
- 현재 곡으로 이동하면 목록이 자동으로 위로 올라오도록 스크롤 동기화를 추가했다.
- 플레이리스트 상세의 YouTube 이동은 제목 클릭으로 옮기고, 버튼은 제거했다.
- 플레이리스트 내부 곡 수를 더 많이 받아오도록 백엔드 반환 개수를 늘렸다.

### 확인할 것

- 플레이리스트 탭에서 목록이 세로로 스크롤되는지 확인한다.
- 하단 이전/다음 버튼을 누르면 강조 표시가 바뀌는지 확인한다.
- 재생 곡이 바뀌면 목록이 자동으로 위쪽으로 이동하는지 확인한다.
- 플레이리스트 제목을 클릭하면 YouTube로 이동하는지 확인한다.

## 2026-05-26 플레이리스트 곡 메타데이터 연동

### 작업 목적

플레이리스트의 곡을 단순한 문자열이 아니라 이미지와 링크를 가진 곡 데이터로 바꿔서, 현재 재생 곡에 맞춰 화면이 함께 바뀌도록 했다.

### 작업 내용

- 플레이리스트 트랙 응답을 곡 제목만 담는 문자열 배열에서 곡 메타데이터 배열로 바꿨다.
- 각 곡은 제목, 썸네일, YouTube 주소, 영상 ID를 함께 갖도록 정리했다.
- `Popular` 플레이리스트 목록에서 곡을 클릭하면 해당 곡이 위로 올라오고 바로 재생되도록 연결했다.
- 현재 재생 중인 플레이리스트 곡의 썸네일이 `Popular`와 하단 플레이바에 반영되도록 했다.
- 추천 곡 카드의 고정 설명 문구를 제거해서 Mood 영역과 정보가 겹치지 않도록 정리했다.
- 하단 플레이바의 곡 제목 영역이 더 넓게 보이도록 레이아웃을 조정했다.

### 확인할 것

- 플레이리스트 항목을 클릭하면 해당 곡으로 바로 재생되는지 확인한다.
- 클릭한 곡이 목록 상단으로 올라오는지 확인한다.
- 현재 재생 곡에 맞춰 `Popular`와 하단 플레이바 이미지가 바뀌는지 확인한다.
- 하단 플레이바의 제목이 이전보다 덜 잘리는지 확인한다.

## 2026-05-26 재생 반복 문제와 Mood 표시 정리

### 작업 목적

플레이리스트 곡 재생이 반복되거나 꺼졌다 켜지는 것처럼 보이던 문제를 줄이고, Mood 영역의 표시 방식도 더 읽기 쉽게 정리했다.

### 작업 내용

- YouTube 플레이어가 같은 영상을 불필요하게 다시 불러오지 않도록 현재 로드된 `video_id`를 기억하는 방식으로 정리했다.
- `PAUSED` 상태를 종료 상태처럼 처리하지 않도록 바꿔서 재생 화면이 불필요하게 꺼지는 흐름을 줄였다.
- 플레이리스트 곡 선택, 이전/다음 이동, 재생/일시정지를 같은 로드 기준으로 맞췄다.
- Mood 영역은 `mood_tags` 기준으로만 보여주고, 줄바꿈되도록 해서 문장이 잘리지 않게 조정했다.

### 확인 결과

- 프론트 `lint`를 통과했다.
- 프론트 `build`를 통과했다.
- 플레이리스트 재생 흐름과 Mood 표시를 다시 확인할 수 있는 상태로 정리했다.

## 2026-05-26 AI 설계 및 API 초안 정리

### 작업 목적

실제 AI 모델 구현 전에, 입력/출력 구조와 API 연결 기준을 문서로 먼저 고정했다.

### 작업 내용

- AI 설계 초안을 감정 태그, 분위기 태그, 검색어 생성 흐름 기준으로 다시 정리했다.
- API 설계 초안을 `POST /api/analyze` 요청과 응답 구조 기준으로 다시 정리했다.
- 현재 목업 분석 함수가 어떤 역할을 대신하는지도 문서에 함께 적었다.

### 확인 결과

- AI 모델 연결 전에 확인해야 할 기준이 더 분명해졌다.
- 백엔드와 프론트가 바라보는 응답 구조를 문서로 맞췄다.

## 2026-05-26 AI 파일 생성 및 백엔드 연결

### 작업 목적

실제 AI 모델이 들어갈 자리를 먼저 만들고, 백엔드 API가 그 구조를 바로 쓰도록 연결했다.

### 작업 내용

- `backend/app/ai/` 폴더를 생성했다.
- `schema.py`, `labels.py`, `preprocess.py`, `model.py`, `predict.py`를 추가했다.
- `POST /api/analyze`가 `predict_analysis()`를 호출하도록 바꿨다.
- 현재는 실제 학습 모델 대신 규칙 기반 초안으로 동작한다.

### 확인 결과

- 백엔드 가상환경 기준으로 `predict_analysis()` import가 정상 동작한다.
- `compileall`로 새 파일들의 문법을 확인했다.
- 나중에 학습 모델만 교체하면 백엔드 흐름은 유지할 수 있다.

## 2026-05-26 AI 학습 모델 선택

### 작업 목적

실제 학습에 사용할 한국어 문장 분류 모델을 정했다.

### 작업 내용

- 1차 학습 모델로 `klue/roberta-small`을 선택했다.
- 하나의 모델에서 감정 태그와 분위기 태그를 함께 예측하는 다중 라벨 분류 구조로 정리했다.
- AI 설계 문서에 선택 이유와 출력 구조를 추가했다.

### 확인 결과

- 실제 모델 구현 방향이 규칙 기반 초안에서 학습 모델 기반 구조로 이어질 수 있게 정리됐다.

## 2026-05-26 ML 학습 폴더 초안 생성

### 작업 목적

실제 AI 모델 학습을 위한 작업 공간을 백엔드 실행 코드와 분리해서 만들었다.

### 작업 내용

- `ml/` 폴더를 추가했다.
- 학습 데이터 예시를 `sample_training_data.jsonl` 형식으로 작성했다.
- 학습된 모델 결과물을 둘 `models/` 폴더를 준비했다.
- 새 라이브러리 설치 없이 학습 준비 구조만 먼저 만들었다.

### 확인 결과

- 백엔드 실행 코드와 학습 코드의 역할이 분리됐다.
- 다음 단계에서 학습 스크립트를 추가할 기준이 생겼다.

## 2026-05-26 AI 학습 의존성 초안 추가

### 작업 목적

`klue/roberta-small` 학습에 필요한 최소 라이브러리를 별도 파일로 정리했다.

### 작업 내용

- `ml/requirements.txt`를 추가했다.
- 1차 학습 의존성으로 `torch`, `transformers`, `scikit-learn`을 정리했다.
- `ml/README.md`에 각 라이브러리가 필요한 이유를 적었다.

### 확인 결과

- 백엔드 실행 의존성과 AI 학습 의존성을 분리해서 관리할 수 있게 됐다.

## 2026-05-26 플레이리스트 곡 목록 지연 조회 적용

### 작업 목적

자연어 입력 후 결과 화면으로 넘어가는 시간을 줄이기 위해 플레이리스트 내부 곡 목록 조회 시점을 늦췄다.

### 작업 내용

- `/api/analyze` 응답에서 모든 플레이리스트의 내부 곡 목록을 미리 조회하지 않도록 변경했다.
- 선택된 플레이리스트의 곡 목록만 가져오는 `/api/playlists/{playlist_id}/tracks` API를 추가했다.
- 프론트엔드는 현재 선택된 플레이리스트에 대해서만 곡 목록을 늦게 요청하도록 변경했다.
- Mock 플레이리스트는 실제 YouTube 플레이리스트 ID가 아니므로 외부 곡 목록 조회를 시도하지 않도록 정리했다.

### 확인 결과

- 결과 화면 진입 전에 발생하던 플레이리스트 내부 곡 조회 요청 수를 줄였다.
- 플레이리스트 곡 목록은 결과 화면이 뜬 뒤 선택된 항목 기준으로 채워지는 구조가 됐다.
- 동일한 테스트 입력 기준 추천 데이터 조회 시간이 약 24초에서 약 4초로 줄어든 것을 확인했다.

## 2026-05-26 YouTube 추천 검색 병렬 처리

### 작업 목적

자연어 분석 후 추천 곡과 추천 플레이리스트를 순서대로 기다리지 않도록 해서 결과 응답 시간을 줄인다.

### 작업 내용

- `/api/analyze`에서 추천 곡 검색과 추천 플레이리스트 검색을 동시에 실행하도록 변경했다.
- 새 라이브러리는 추가하지 않고 Python 기본 `ThreadPoolExecutor`를 사용했다.

### 확인 결과

- 추천 곡과 추천 플레이리스트 응답 구조는 유지했다.
- 서로 독립적인 YouTube 검색 대기 시간을 겹쳐서 처리할 수 있게 됐다.

## 2026-05-26 AI 학습 스크립트 초안 추가

### 작업 목적

감정과 분위기 태그를 예측하는 모델을 학습하기 위한 코드 흐름을 준비한다.

### 작업 내용

- `ml/train.py`를 추가했다.
- JSON Lines 학습 데이터를 읽는 구조를 만들었다.
- 감정 태그와 분위기 태그를 멀티라벨 학습 형식으로 변환하는 코드를 작성했다.
- `klue/roberta-small` 기반 학습 설정 초안을 작성했다.
- `ml/README.md`에 학습 스크립트 흐름을 추가했다.

### 확인 결과

- 실제 학습 실행 전, 데이터 로드부터 모델 저장까지의 전체 흐름을 코드로 확인할 수 있게 됐다.

## 2026-05-26 AI 학습 데이터 라벨링 기준 추가

### 작업 목적

학습 데이터를 늘릴 때 감정 태그와 분위기 태그가 일관되게 붙도록 기준을 정리한다.

### 작업 내용

- `ml/data-labeling-guide.md`를 추가했다.
- 감정 태그와 분위기 태그의 의미를 정리했다.
- 복합 감정 문장을 라벨링하는 기준을 추가했다.
- 피해야 할 라벨링 방식과 좋은 예시를 작성했다.

### 확인 결과

- 학습 데이터를 추가하기 전에 태그 기준을 먼저 확인할 수 있게 됐다.

## 2026-05-26 KOTE 기반 AI 학습 방향 반영

### 작업 목적

직접 만든 감정/분위기 태그를 학습하는 대신 공개 데이터셋인 KOTE의 감정 라벨을 기준으로 모델을 학습하는 방향으로 정리한다.

### 작업 내용

- AI 모델은 KOTE 감정 라벨만 예측하도록 학습 방향을 수정했다.
- 분위기 태그는 모델이 직접 예측하지 않고 백엔드 추천 로직에서 변환하는 구조로 정리했다.
- `ml/data-labeling-guide.md`를 KOTE 라벨 활용 기준 문서로 다시 작성했다.
- `ml/train.py`가 `labels` 필드를 학습하도록 수정했다.
- 샘플 학습 데이터 형식을 KOTE 라벨 구조에 맞게 변경했다.

### 확인 결과

- 감정 분석 모델과 음악 추천 로직의 역할이 분리됐다.
- 공개 데이터셋의 라벨 구조를 살리면서 프로젝트 추천 방향을 유지할 수 있는 구조가 됐다.

## 2026-05-26 KOTE 데이터 변환 스크립트 초안 추가

### 작업 목적

KOTE 원본 TSV 파일을 프로젝트 학습 스크립트가 읽을 수 있는 JSONL 형식으로 변환할 수 있게 준비한다.

### 작업 내용

- `ml/data/raw/` 폴더를 추가했다.
- `ml/prepare_kote.py`를 추가했다.
- `train.tsv`, `val.tsv`, `test.tsv`를 읽어 `kote_training_data.jsonl`로 변환하는 흐름을 만들었다.
- 새 라이브러리 없이 Python 기본 `csv`, `json`, `ast` 모듈만 사용했다.
- `ml/README.md`에 KOTE 데이터 준비 흐름을 추가했다.

### 확인 결과

- KOTE 원본 파일을 직접 받으면 별도 의존성 없이 학습용 JSONL로 변환할 수 있는 구조가 됐다.
- 원본 TSV가 없을 때는 필요한 파일 위치를 안내하는 오류가 발생하는 것을 확인했다.

## 2026-05-26 KOTE 원본 데이터 다운로드 및 변환

### 작업 목적

KOTE 공개 데이터셋을 실제 학습에 사용할 수 있도록 로컬에 내려받고 JSONL 학습 데이터로 변환한다.

### 작업 내용

- KOTE `train.tsv`, `val.tsv`, `test.tsv`를 `ml/data/raw/`에 다운로드했다.
- KOTE TSV가 헤더 없이 `id`, `text`, `labels` 순서로 구성된 것을 확인했다.
- 숫자 라벨 인덱스를 KOTE 감정 라벨 이름으로 변환하도록 `ml/prepare_kote.py`를 수정했다.
- `ml/data/kote_training_data.jsonl`을 생성했다.
- 원본 TSV와 변환 JSONL이 Git에 포함되지 않도록 `.gitignore`에 제외 규칙을 추가했다.
- `ml/train.py`의 기본 학습 데이터 경로를 `kote_training_data.jsonl`로 변경했다.

### 확인 결과

- 총 50,000개의 KOTE 문장이 JSONL로 변환됐다.
- 변환된 데이터는 `text`와 `labels` 구조를 가진다.
- KOTE 원본 TSV와 변환 JSONL은 Git 추적 대상에서 제외되는 것을 확인했다.

## 2026-05-26 모델 산출물 Git 제외 정책 정리

### 작업 목적

실제 학습을 실행했을 때 생성되는 모델 파일과 체크포인트가 Git 저장소에 섞이지 않도록 정리한다.

### 작업 내용

- `.gitignore`에 `ml/models/*` 제외 규칙을 추가했다.
- `ml/models/.gitkeep`은 유지되도록 예외 규칙을 추가했다.
- `ml/README.md`에 모델 산출물 관리 기준을 작성했다.

### 확인 결과

- 모델 학습 결과물은 로컬 작업 공간에만 남고 Git에는 포함되지 않는 구조가 됐다.

## 2026-05-26 AI 학습 환경 준비

### 작업 목적

CPU 기준으로 KOTE 학습 파이프라인을 검증할 수 있는 별도 ML 실행 환경을 준비한다.

### 작업 내용

- 백엔드 가상환경과 분리된 `ml/.venv` 사용 방향을 정리했다.
- `ml/train.py`에 `--max-samples` 옵션을 추가했다.
- 처음에는 전체 50,000개 데이터가 아니라 일부 샘플로 학습 흐름을 확인하도록 문서화했다.
- `accelerate`를 학습 의존성에 추가했다.
- `transformers 5.9.0`에 맞춰 `TrainingArguments`와 `Trainer` 사용 방식을 수정했다.
- `--max-samples 20`으로 작은 샘플 학습을 실행했다.

### 확인 결과

- 전체 학습 전에 작은 데이터로 학습 코드와 모델 저장 흐름을 점검할 수 있게 됐다.
- `torch 2.12.0+cpu`, `transformers 5.9.0`, `scikit-learn 1.8.0`, `accelerate 1.13.0` 설치를 확인했다.
- CUDA는 사용하지 않는 CPU 학습 환경으로 확인됐다.
- 샘플 20개 기준 학습과 모델 저장이 끝까지 성공했다.

## 2026-05-26 AI 샘플 추론 스크립트 추가

### 작업 목적

학습된 로컬 모델이 실제 문장을 입력받아 KOTE 감정 라벨을 예측하는지 확인한다.

### 작업 내용

- `ml/predict_sample.py`를 추가했다.
- 저장된 모델과 토크나이저를 불러오는 구조를 만들었다.
- `labels.json`을 읽어 모델 출력 인덱스를 감정 라벨 이름으로 변환하도록 했다.
- 멀티라벨 분류 결과를 `sigmoid` 점수 기준으로 정렬해 출력하도록 했다.
- `ml/README.md`에 샘플 추론 실행 방법을 추가했다.

### 확인 결과

- 학습된 로컬 모델로 문장을 입력했을 때 KOTE 감정 라벨과 점수가 출력되는 구조가 됐다.

## 2026-05-26 AI 평가 스크립트 초안 추가

### 작업 목적

학습된 로컬 모델의 예측 결과를 숫자 지표로 확인할 수 있게 한다.

### 작업 내용

- `ml/evaluate.py`를 추가했다.
- 저장된 모델과 라벨 목록을 불러와 검증 데이터를 예측하도록 했다.
- 멀티라벨 예측을 위해 `sigmoid` 점수와 threshold를 사용했다.
- `precision_micro`, `recall_micro`, `f1_micro`, `f1_macro`를 출력하도록 했다.
- `ml/README.md`에 평가 실행 방법을 추가했다.

### 확인 결과

- 작은 샘플 모델 기준으로도 평가 지표를 계산할 수 있는 구조가 됐다.

## 2026-05-26 KOTE 500개 샘플 학습 실행

### 작업 목적

20개 샘플보다 큰 500개 샘플로 CPU 학습을 실행해 학습 파이프라인의 안정성을 확인한다.

### 작업 내용

- `ml/train.py`에서 중간 체크포인트 저장을 끄고 최종 모델만 저장하도록 변경했다.
- `--max-samples 500` 옵션으로 KOTE 샘플 학습을 실행했다.
- 학습 완료 후 `ml/evaluate.py --max-samples 500`으로 평가 지표를 확인했다.
- 학습된 모델로 샘플 문장 추론을 실행했다.

### 확인 결과

- 500개 샘플 학습이 약 2분 56초 만에 완료됐다.
- 최종 train loss는 약 `0.4365`로 확인됐다.
- 평가 결과는 `precision_micro 0.7342`, `recall_micro 0.0781`, `f1_micro 0.1411`, `f1_macro 0.0416`으로 확인됐다.
- 모델 산출물은 Git 추적 대상에서 제외된 상태로 유지됐다.

## 2026-05-26 AI 평가 threshold 비교 기능 추가

### 작업 목적

멀티라벨 감정 분류에서 threshold 값에 따라 precision, recall, F1이 어떻게 달라지는지 확인한다.

### 작업 내용

- `ml/evaluate.py`에 `--thresholds` 옵션을 추가했다.
- 모델 예측 점수를 한 번만 계산하고 여러 threshold에 재사용하도록 변경했다.
- `ml/README.md`에 threshold 비교 실행 예시를 추가했다.

### 확인 결과

- `0.3`, `0.4`, `0.5` threshold를 한 번에 비교할 수 있게 됐다.
- 500개 샘플 모델 기준 `threshold 0.3`에서 `f1_micro 0.4240`으로 가장 높게 나왔다.

## 2026-05-26 AI 모델 서비스 연결 설계 추가

### 작업 목적

학습된 KOTE 감정 분석 모델을 백엔드 서비스에 연결하기 전에 연결 기준을 정리한다.

### 작업 내용

- `docs/12-ai-service-integration.md`를 추가했다.
- KOTE 감정 라벨을 기존 API 응답 구조에 어떻게 넣을지 정리했다.
- 모델 파일이 없을 때 기존 규칙 기반 분석으로 fallback하는 기준을 정했다.
- 모델 lazy loading 방식과 기본 threshold `0.3` 기준을 문서화했다.
- KOTE 감정 라벨을 서비스용 분위기 태그로 변환하는 매핑 초안을 정리했다.

### 확인 결과

- 백엔드 코드 연결 전에 모델 파일 위치, fallback, threshold, 라벨 매핑 기준이 정리됐다.

## 2026-05-26 KOTE 라벨 분위기 매핑 추가

### 작업 목적

KOTE 감정 라벨을 기존 API 응답 구조의 `mood_tags`와 `search_keywords`로 변환할 수 있게 한다.

### 작업 내용

- `backend/app/ai/mood_mapping.py`를 추가했다.
- KOTE 감정 라벨 → 서비스 분위기 태그 매핑을 작성했다.
- 기존 규칙 기반 영어 감정 라벨도 같은 매핑 함수에서 처리하도록 했다.
- `predict_analysis()`가 모델 결과의 `mood_tags`가 없으면 감정 라벨 기준으로 분위기 태그를 만들도록 변경했다.
- AI 서비스 연결 설계 문서에 1차 구현 상태를 추가했다.

### 확인 결과

- 모델 연결 전에도 기존 규칙 기반 분석 흐름이 유지된다.
- 이후 KOTE 모델이 감정 라벨만 반환해도 프론트가 사용하는 `mood_tags`와 `search_keywords`를 만들 수 있는 구조가 됐다.

## 2026-05-26 KOTE 모델 어댑터 추가

### 작업 목적

백엔드에서 로컬 학습 모델이 있으면 KOTE 모델을 사용하고, 없으면 기존 규칙 기반 모델로 fallback하도록 한다.

### 작업 내용

- `backend/app/ai/kote_adapter.py`를 추가했다.
- 로컬 모델 파일과 `labels.json`을 읽어 KOTE 감정 라벨을 예측하는 어댑터를 만들었다.
- `load_model()`에서 모델 파일이 있으면 `KoteModelAdapter`를 사용하도록 변경했다.
- 모델 파일이 없거나 `torch/transformers` 의존성이 없으면 기존 `AiModel`을 사용하도록 했다.
- `lru_cache`로 모델을 한 번만 로드하도록 했다.
- AI 서비스 연결 설계 문서에 모델 어댑터 구현 상태를 추가했다.

### 확인 결과

- 백엔드가 모델 파일 유무에 따라 KOTE 모델과 규칙 기반 모델을 선택할 수 있는 구조가 됐다.
