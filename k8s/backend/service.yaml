apiVersion: v1
kind: Service
metadata:
  name: youtube-mood-backend
  namespace: youtube-mood
spec:
  selector:
    app: youtube-mood-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
