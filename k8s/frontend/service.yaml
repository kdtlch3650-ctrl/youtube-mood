apiVersion: v1
kind: Service
metadata:
  name: youtube-mood-frontend
  namespace: youtube-mood
spec:
  selector:
    app: youtube-mood-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
